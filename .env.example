# Local preview (uses the explicitly unverified bundled fixture)
DATA_REPOSITORY=bundled-preview

# Production/staging: set DATABASE_URL and use the PostgreSQL adapter.
# The API refuses to fall back to bundled data when this variable is present.
# DATA_REPOSITORY=postgresql
# DATABASE_URL=postgres://user:password@host:5432/vgc_teambuilder
